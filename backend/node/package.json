{
  "name": "codeguard-security-gateway",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "gateway:dev": "tsx src/server.ts",
    "gateway:build": "tsc -p tsconfig.json",
    "gateway:start": "node dist/server.js",
    "gateway:typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.16.5",
    "tsx": "^4.20.6",
    "typescript": "^5.8.3"
  }
}
